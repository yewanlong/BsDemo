<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="simcpux.sourceforge.net.muzilibrary">

    <application
        android:name="simcpux.sourceforge.net.InitApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity
            android:name="simcpux.sourceforge.net.muzilibrary.activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="simcpux.sourceforge.net.muzilibrary.activity.img.ImageActivity" />
        <activity android:name="simcpux.sourceforge.net.muzilibrary.activity.choose.ChooseActivity" />
        <activity android:name=".activity.pay.PayActivity" />
        <activity android:name=".wxapi.WXEntryActivity" />
        <activity android:name=".wxapi.WXPayEntryActivity" />
        <activity android:name=".activity.list.ListActivity" />
        <activity android:name=".activity.list.ListActivity1" />
        <activity android:name=".activity.list.ListActivity2" />
        <activity android:name=".activity.msg.ModifyPassActivity" />
        <activity android:name=".activity.msg.MsgActivity" />

    </application>

</manifest>